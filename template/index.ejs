<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="description" content="<%=data.others.description%>">
  <meta name="keywords" content="<%=data.others.description%>">
  <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no">

  <title><%=data.title%></title>
  <link rel="shortcut icon" href="/images/favicon.ico" sizes="16x16">
  <link rel="canonical" href="<%=data.url%>">
  <link rel="stylesheet" href="/build/main.css">
</head>

<body>
  <div id="root"><%-data.html%></div>

  <script>
    window.__PRELOADED_STATE__ = <%-JSON.stringify(data.preloadedState).replace(/</g, '\\u003c')%>
  </script>

  <script src="/js/react.development.js"></script>
  <script src="/js/react-dom.development.js"></script>
  <script src="/build/main.bundle.js"></script>
  <%-
    data.bundles.map(function(bundle) {
      if(!bundle.file.match(/.+map$/))
        return `<script src="/build/${bundle.file}"></script>`;
      else
        return '';
    }).join('\n')
  %>
  
</body>

</html>